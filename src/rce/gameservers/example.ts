import { ServerOptions } from "rce.js/dist/types";

const example: ServerOptions = {
  identifier: "example",
  region: "US",
  serverId: 1,
  refreshPlayers: 5,
  state: ["example_state", "example_state_2"],
};

export default example;
