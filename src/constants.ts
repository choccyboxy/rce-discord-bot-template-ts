export enum CommandCategory {
  DEBUG = "Debug",
  DEVELOPER = "Developer",
  GENERAL = "General",
}

export enum Emoji {
  CHECK = "✅",
  CROSS = "❌",
  LOADING = "🔄",
  WARNING = "⚠️",
}

export enum RCERanks {
  OWNER = "Owner",
  ADMIN = "Admin",
  MODERATOR = "Moderator",
  VIP = "VIP",
  MEMBER = "Member",
}

export enum LogType {
  INFO = "INFO 💬",
  WARN = "WARN 🚨",
  ERROR = "ERROR ❌",
  BOT = "BOT 🤖",
}
